# Finnhub API key - get a free one at https://finnhub.io/register
FINNHUB_API_KEY=your_finnhub_key_here

# Optional overrides
ACTIVITYWATCH_URL=http://localhost:5600/api/0
PORT=5000

# Optional: raises the GitHub API rate limit for the projects card.
# A classic token with NO scopes is enough - only public data is requested.
GITHUB_TOKEN=
GITHUB_USER=milo475
